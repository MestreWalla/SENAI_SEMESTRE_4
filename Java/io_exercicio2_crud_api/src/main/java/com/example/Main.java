package com.example;

public class Main {
    public static void main(String[] args) {
        UsuarioController controller = new UsuarioController();
        controller.read();
        // controller.write(new Usuario("bdbe", "Maycon", 32, "Rua x"));
        // controller.update(new Usuario("bdbe", "NomeNovo", 32, "Rua Nova"));
        // controller.delete("d634");
        
    }
}